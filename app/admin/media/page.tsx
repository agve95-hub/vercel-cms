import { db, schema } from "@/lib/db"; import { desc } from "drizzle-orm"; import { Image } from "lucide-react";
export default async function MediaPage() { const m = await db.select().from(schema.media).orderBy(desc(schema.media.createdAt)); return (<div><h1 className="text-2xl font-bold mb-6">Media Library</h1>{m.length===0?<div className="bg-white rounded-xl border p-12 text-center text-gray-500"><Image size={40} className="mx-auto mb-3 text-gray-300"/><p>No media yet.</p></div>:<div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">{m.map(i=>(<div key={i.id} className="bg-white rounded-lg border overflow-hidden"><div className="aspect-square bg-gray-100"><img src={`/${i.filepath}`} alt={i.altText||i.filename} className="w-full h-full object-cover"/></div><div className="p-2"><p className="text-xs text-gray-600 truncate">{i.filename}</p></div></div>))}</div>}</div>); }
