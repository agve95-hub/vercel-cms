import { db, schema } from "@/lib/db"; import { desc } from "drizzle-orm"; import { format } from "date-fns";
export default async function ActivityPage() { const a = await db.select().from(schema.activityLog).orderBy(desc(schema.activityLog.createdAt)).limit(100); return (<div><h1 className="text-2xl font-bold mb-6">Activity Log</h1><div className="bg-white rounded-xl border overflow-hidden"><table className="w-full"><thead><tr className="border-b bg-gray-50"><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Action</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Title</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Date</th></tr></thead><tbody className="divide-y">{a.map(r=>(<tr key={r.id}><td className="px-4 py-3 text-sm capitalize">{r.action}</td><td className="px-4 py-3 text-sm text-gray-500 capitalize">{r.entityType}</td><td className="px-4 py-3 text-sm">{r.entityTitle}</td><td className="px-4 py-3 text-sm text-gray-400">{r.createdAt?format(new Date(r.createdAt),"MMM d, h:mm a"):""}</td></tr>))}</tbody></table></div></div>); }
