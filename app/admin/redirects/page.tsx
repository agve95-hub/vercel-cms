import { db, schema } from "@/lib/db"; import { desc } from "drizzle-orm";
export default async function RedirectsPage() { const r = await db.select().from(schema.redirects).orderBy(desc(schema.redirects.createdAt)); return (<div><h1 className="text-2xl font-bold mb-6">Redirects</h1><div className="bg-white rounded-xl border overflow-hidden">{r.length===0?<div className="p-12 text-center text-gray-500">No redirects.</div>:<table className="w-full"><thead><tr className="border-b bg-gray-50"><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">From</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">To</th><th className="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Hits</th></tr></thead><tbody className="divide-y">{r.map(x=>(<tr key={x.id}><td className="px-4 py-3 text-sm font-mono">{x.sourcePath}</td><td className="px-4 py-3 text-sm font-mono">{x.destination}</td><td className="px-4 py-3 text-sm text-gray-400">{x.hits}</td></tr>))}</tbody></table>}</div></div>); }
